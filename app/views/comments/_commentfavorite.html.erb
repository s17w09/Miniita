<% if logged_in? %>
  <% if @comment.favorited?(current_user) %>
    <%= render 'comments/unfavorite', commentfavorite: commentfavorite, comment: comment %>
  <% else %>
    <%= render 'comments/favorite', commentfavorite: commentfavorite, comment: comment %>
  <% end %>
<% else %>
  <%= link_to login_path do %>
    <i class="bi bi-arrow-through-heart"></i>
    <%= comment.favorites.count %>
  <% end %>
<% end %>
