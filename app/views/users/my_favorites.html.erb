<% content_for :title, page_title('いいねした記事') %>
<div class="w-5/6 mx-auto max-w-screen-xl flex-grow mb-20">
  <div class="mt-20">
    <h1 class="mb-10 text-3xl font-bold text-gray-600">いいねした記事</h1>
    <p class="ml-3 text-xl font-bold text-gray-600 mb-1"><i class="bi bi-hand-thumbs-up"></i> 参考になった</p>
    <% @like_first.each do |favorite| %>
      <div class="p-4 border rounded-lg mb-3 bg-stone-50 float1">
        <%= link_to article_path(favorite.article), class: 'app-link' do %>
          <p class="pl-4 font-semibold text-xl mt-2 text-gray-600"><%= favorite.article.title %></p>
        <% end %>
      </div>
    <% end %>

    <p class="ml-3 mt-10 text-xl font-bold text-gray-600 mb-1"><i class="bi bi-award"></i> 学びが深まった</p>
    <% @like_second.each do |favorite| %>
      <div class="p-4 border rounded-lg mb-3 bg-stone-50 float1">
        <%= link_to article_path(favorite.article), class: 'app-link' do %>
          <p class="pl-4 font-semibold text-xl mt-2 text-gray-600"><%= favorite.article.title %></p>
        <% end %>
      </div>
    <% end %>

    <p class="ml-3 mt-10 text-xl font-bold text-gray-600 mb-1"><i class="bi bi-arrow-through-heart"></i> 解説が丁寧</p>
    <% @like_third.each do |favorite| %>
      <div class="p-4 border rounded-lg mb-3 bg-stone-50 float1">
        <%= link_to article_path(favorite.article), class: 'app-link' do %>
          <p class="pl-4 font-semibold text-xl mt-2 text-gray-600"><%= favorite.article.title %></p>
        <% end %>
      </div>
    <% end %>

    <p class="ml-3 mt-10 text-xl font-bold text-gray-600 mb-1"><i class="bi bi-brightness-alt-high"></i> 独自の視点</p>
    <% @like_forth.each do |favorite| %>
      <div class="p-4 border rounded-lg mb-3 bg-stone-50 float1">
        <%= link_to article_path(favorite.article), class: 'app-link' do %>
          <p class="pl-4 font-semibold text-xl mt-2 text-gray-600"><%= favorite.article.title %></p>
        <% end %>
      </div>
    <% end %>

    <p class="ml-3 mt-10 text-xl font-bold text-gray-600 mb-1"><i class="bi bi-bell"></i> 詳細をもっと知りたい</p>
    <% @like_fifth.each do |favorite| %>
      <div class="p-4 border rounded-lg mb-3 bg-stone-50 float1">
        <%= link_to article_path(favorite.article), class: 'app-link' do %>
          <p class="pl-4 font-semibold text-xl mt-2 text-gray-600"><%= favorite.article.title %></p>
        <% end %>
      </div>
    <% end %>

  </div>
</div>
